{
  "executors": {
    "esbuild": {
      "implementation": "./src/executors/esbuild/executor",
      "schema": "./src/executors/esbuild/schema.json",
      "description": "Runs esbuild with a config provided by the root of the repository"
    },
    "clean": {
      "implementation": "./src/executors/clean/executor",
      "schema": "./src/executors/clean/schema.json",
      "description": "Clean a directory"
    },
    "vite-build": {
      "implementation": "./src/executors/vite-build/executor",
      "schema": "./src/executors/vite-build/schema.json",
      "description": "Vite build"
    }
  }
}
